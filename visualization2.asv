clc;
clear;

dataString = fileread('resultdata.txt');
fullData = sscanf(dataString(10:end), '%d;%f;%f;%f\n', [4 Inf]);

trackedBodies = max(fullData(1, :)) + 1;
dataCount = size(fullData, 2)/(max(fullData(1, :)) + 1);

for i = 1:37:dataCount-37
    scatter3(fullData(2, ));
end
